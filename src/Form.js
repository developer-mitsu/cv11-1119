import React from 'react'

const Form = () => {
    return (
        <React.Fragment>
            <input type="text"/>
            <button>追加する</button>
        </React.Fragment>
    )
}

export default Form